#+title: Agenda Notebook
xs#+author: Marcus Birkenkrahe
#+subtitle: CSC 100 [Spring 2026]
#+SEQ_TODO: TODO NEXT IN-PROGRESS PRACTICE | DONE CANCELLED
#+startup: overview hideblocks indent entitiespretty
#+property: header-args:C      :main yes :includes <stdio.h> :results output :exports both
#+property: header-args:python :session *Python* :python python3 :results output :exports both
#+property: header-args:R      :session *R* :results graphics output file :exports both
#+property: header-args:C++    :main yes :includes <iostream> :results output :exports both
* Week 1: Entry survey + test / Overview
#+attr_html: :width 300px:
#+caption: Patriotic poster for 2026
[[../img/cover.png]]

- [X] [[https://docs.google.com/forms/d/1PQ3gLUjisIdIImMny23hvrRmI7UU2GfuFD8DQFykvtU/edit][Entry survey + video + test]] - Questions?
- [X] Glossary of terms used (exemplary use of AI)
- [X] New: AI Video/Poster Syllabus Overview
- [X] Lecture & practice: Overview of the course CSC 100
- [X] Reading assignment: Think C Chapter 1
- [X] Introduction to the course topic - C/C++
- [ ] Practice: OneCompiler.com

** Glossary - keywords you saw in the video briefly explained

| No. | Term              | Explanation                         |
|-----+-------------------+-------------------------------------|
|   1 | C                 | Programming language discussed      |
|   2 | statically typed  | Types checked at compile time       |
|   3 | procedural        | Step-by-step style using functions  |
|   4 | Bell Labs         | Place where C was created           |
|   5 | Unix              | OS developed using C                |
|   6 | C++               | Language influenced by C            |
|   7 | C#                | Language influenced by C            |
|   8 | Java              | Language influenced by C            |
|   9 | JavaScript        | C-like syntax language              |
|  10 | Perl              | Language mentioned as influenced    |
|  11 | PHP               | Language mentioned as influenced    |
|  12 | compiler          | Translates source code              |
|  13 | GCC               | Popular C compiler                  |
|  14 | file              | Source code stored on disk          |
|  15 | .c                | C source file extension             |
|  16 | library           | Reusable code collection            |
|  17 | include           | Adds libraries to a program         |
|  18 | main function     | Program entry point                 |
|  19 | function          | Named block of code                 |
|  20 | integer           | Whole-number type                   |
|  21 | return value      | Value sent back by a function       |
|  22 | types             | Basic data categories               |
|  23 | variable          | Named storage for a value           |
|  24 | printf            | Prints to standard output           |
|  25 | standard output   | Default output stream               |
|  26 | & (ampersand)     | Refers to an address                |
|  27 | address           | Location in memory                  |
|  28 | memory            | Storage used by programs            |
|  29 | pointer           | Variable holding an address         |
|  30 | * (star)          | Used in pointer declarations        |
|  31 | allocate          | Reserve memory                      |
|  32 | bytes             | Unit of memory size                 |
|  33 | index             | Position in an array                |
|  34 | char              | Single-character type               |
|  35 | string            | Array of chars ending in null       |
|  36 | array             | Sequence of elements                |
|  37 | null character    | Marks end of a string               |
|  38 | garbage collector | Automatic cleanup (not in C)        |
|  39 | memory leak       | Memory not released                 |
|  40 | free              | Releases allocated memory           |
|  41 | RAM               | Main computer memory                |
|  42 | machine code      | CPU instructions                    |
|  43 | Operating system  | Software managing hardware          |
|  44 | CPU               | Processor executing instructions    |
|  45 | hardware          | Physical computer components        |
|  46 | object-oriented   | Paradigm C does not support         |
|  47 | data types        | User-defined structures (mentioned) |
|  48 | Linux             | OS kernel written largely in C      |
|  49 | Windows           | OS whose kernel uses C              |
|  50 | Dennis Ritchie    | Creator of the C language           |

** How to work with AI - three examples

- To compile this glossary, I copied the transcript of the video into
  a ChatGPT conversation and asked for the glossary using this prompt:

  #+begin_example
  You are given the transcript of a short introductory video about the
  C programming language.

  Task:
  1. Extract glossary terms that are explicitly mentioned in the transcript.
  2. Create a glossary table with three columns: a running number, the term,
     and a short explanation.
  3. Explanations should be concise (one short sentence), suitable for beginners.
  4. Do not introduce concepts that are not directly stated in the transcript.
  5. Use Org-mode table formatting.
  6. Keep wording simple and faithful to the video.

  Output only the glossary table in a code verbatim window, nothing else.
  #+end_example

- To produce this poster and the slides that you can find in GitHub, I
  used the Syllabus and my first overview lecture with NotebookLM:
  #+attr_html: :width 400px :float nil:
  [[../img/1_overview_poster.png]]

- I also used NotebookLM to produce a set of flashcards from the
  lecture. This would be a good way to study difficult material - less
  computer science (little to memorize) but history for example.
  #+attr_html: :width 300px :float nil:
  [[../img/flashcardFront.png]]  [[../img/flashcardBack.png]]

- We will explore this more as soon as we get into coding.

- You should start your own NotebookLM, fill it with sources from the
  course and use the different available tools.
  #+attr_html: :width 400px :float nil:
  [[../img/notebooklm.png]]

** What to do before/after class
#+attr_html: :width 400px :float nil:
[[../img/1_mastery.png]]

- This is what I do after class (as soon as I'm home):

  1) I extract 5-10 review questions from the lecture, the whiteboard
     screenshots, the practice, and from my memory.

  2) I write the review questions down and try to find answers. This
     is how we will start the next session.

  3) I let AI draft a multiple choice quiz from all the material
     discussed or looked at (for the next online test).

- This is what I do (right) before the next class (still at home):

  1) I try to answer the review questions myself without looking at
     the answers.

  2) I look at the whiteboard screenshots to remember what we
     did. Sometimes I have to look at the session recording.

  3) I check the Google chat to see if I missed any interesting post.

- Since you're not teaching this class you don't have to do exactly
  this, but you need to make sure to check your understanding (in
  whatever tiny way) both not too long after the last, and not too
  close to the next session.

** Review

[Review questions generated by ChatGPT using Whiteboard screenshots]

1. Are all problems computational problems?
   #+begin_quote
   No. Only problems that can be precisely described and solved by a
   step-by-step procedure are computational. Most problems in the
   world are arguably not of that type. They're more complex.
   #+end_quote

2. Is Computer Science actually a science? What's a science?
   #+begin_quote
   Yes and no. A science is a systematic study that builds and tests
   explanations; computer science studies computation, algorithms, and
   information systematically.

   At the same time, it's very young (< 100 years compared to math or
   physics > 2,500 years) and in practice resembles a craft or an art
   more than a science.
   #+end_quote

3. What's GitHub?
   #+begin_quote
   GitHub is an online platform for storing, sharing, and
   collaborating on code using *version control*. It's the largest
   development platform for software engineers/developers.
   #+end_quote

4. How many (reserved) words does C have compared to English?
   #+begin_quote
   C has only a few dozen reserved words (roughly 30), while English
   has hundreds of thousands of words.
   #+end_quote

5. What is "NotebookLM"? What can you use it for?
   #+begin_quote
   NotebookLM is an AI tool that works with your own documents to
   generate summaries, flashcards, questions, and explanations.
   #+end_quote

* Week 2 ("MLK week"): The Way of the Program
#+attr_html: :width 400px :float nil:
#+caption: Plato's Cave
[[../img/cave.png]]

- [X] Test preview / week review
- [X] C Programming language
- [X] Online programming platforms
- [X] Compiling with onecompiler.com
- [X] Uploading an in-class assignment
- [X] Assignments for next week
- [X] Open assignments: *Quiz 1* & *Assignment 1* & *Reading* (Monday)
- [X] Introduction to OneCompiler.com

** Review week 1 (whiteboard)

These review questions were auto-generated by AI using the whiteboard
screenshots available online in GDrive (Canvas > Pages > Course
links).

1. What does *algorithmic thinking* mean in the context of computer
   science?
   #+begin_quote
   Algorithmic thinking is the practice of formulating problems as
   precise, step-by-step procedures that a computer can execute. It's
   what machines require to solve problems.
   #+end_quote

2. What is a *heuristic*, and how does it differ from a guaranteed
   algorithm?
   #+begin_quote
   A heuristic is what humans use to identify problems, understand
   them, and work toward solutions, often without formal guarantees of
   correctness.
   #+end_quote

3. What role does *C* play in the software *stack* compared to
   higher-level languages?
   #+begin_quote
   C sits close to the operating system and hardware, providing
   efficient, low-level control while still supporting structured
   programming.
   #+end_quote

4. What is the purpose of a *shell* (terminal) in an operating system?
   #+begin_quote
   The shell provides a text-based interface that allows users to
   issue commands to the operating system and run programs.
   #+end_quote

5. Why are there far fewer *reserved words* in C than in natural
   languages like English?
   #+begin_quote
   Programming languages are designed to be minimal and unambiguous,
   whereas natural languages prioritize expressiveness and
   flexibility.
   #+end_quote

** Review/Test preview (Way of the Program)

- The review questions are related to one or more questions on the
  weekly tests. In this case they come from the chapter you just read.

- If you did not read the book, and you don't already know
  programming, you won't know the answers: make sure you read it
  later!

-----

1. What's the difference between interpreted and compiled languages?
   Examples for such languages?
   #+begin_quote
   *Interpreted* languages (like Python) go straight from /source code/ to
   result, while *compiled* languages (like C++) require an intermediate
   step, machine or /object/ code, before they can be run.
   #+end_quote

2. Who does what with what until a C program can be run?
   #+begin_quote
   1) The *user* write /source code/ in an *editor*.
   2) The *user* saves the source code in a =.c= *file*.
   3) The *compiler* translates the =.c= file into *object code* (=a.out=)
   4) The *shell* program executes the *object* (or *machine*) *code*.
   #+end_quote

3. What is the anatomy (dissection) of this command: =printf("Hello\n");=
   #+begin_quote
   1) =printf= is a function defined in the =<stdio.h>= header file.
   2) The function argument is a string ="..."=.
   3) The string consists of a /string literal/ (constant) =Hello= and a
   new line character, =\n=.
   4) The command is delimited by a =;= character.
   #+end_quote

4. Which programming errors do you know, when do they occur, and do
   you have an example?
   #+begin_quote
   1) *Syntax errors* when the language rules are violated, for example
      a missing semicolon - program will not compile.
   2) *Logical errors* when your algorithm does not work, for example
      the wrong formula - program will not crash.
   3) *Run-time errors* when the program is executed - for example when
      you divide by zero - program crashes or behaves unexpectedly.
   4) *Semantic error* when program compiles and runs but produces
      incorrect results due to incorrect meaning - for example, using
      a variable incorrectly.

   *Exercise:* write little programs that produce each of these
   errors. - this is exercise 1.3 in Think C on page 10.
   #+end_quote

5. Explain the structure of a typical *C program*.
   #+begin_src C
     // preprocessing: include header files for input/output
     #include <stdio.h> // include header file for input/output

     // main program:
     int main(void)
     {                  // delimiter - start of main program
       // actionxs
       puts("Hello world\n");
       puts("Hello world");
       // confirm success
       return 0;
     }
   #+end_src

   #+RESULTS:
   : Hello world
   :
   : Hello world

6. What is "parsing"?
   #+begin_quote
   Analyzing the structure of a program - e.g. finding =main= to mark
   the beginning of the program, or =printf= to begin printing. The
   compiler must parse the source code to know what to do with it.
   #+end_quote

7. Does C code have to be indented like this to work?
   #+begin_src C
     int main()
     {
       float pi = 3.14;
       printf("%.2f",pi);
     }
   #+end_src

8. Do you always have to have a =main= program?
   #+begin_quote
   Yes.
   #+end_quote

9. What's *debugging*?
   #+begin_quote
   Finding and fixing programming mistakes. Helpful: using a debugging
   program like =gdb= ("GNU Debugger") that allows you to step through
   the code to print intermediate results.
   #+end_quote

10. What does ~EXIT_SUCCESS~ mean (in the example program in the book)?
    #+begin_quote
    ~EXIT_SUCCESS~ is a standard C macro (from ~<stdlib.h>~) that
    indicates a program terminated successfully, typically with exit
    status 0.
    #+end_quote

** Assignments of the week

1. Complete a simple extension of the ="hello world"= program: Comment
   every line of the program and upload the URL to Canvas (by Monday)

2. Read the second chapter (=Variables and Types=): 8 pages (you'll be
   tested on the content in test 2 at the end of the week - Monday).

3. There are *two bonus assignments*, which you can tackle at any point
   between now and the last day of class. One is simple, the other one
   is not so simple.

** Review (Wednesday)

[These review questions were generated by ChatGPT from the video
transcript - with a little bit of back and forth to sharpen them.]

1. Why is feeling confused or uncomfortable a normal part of learning
   C?
   #+begin_quote
   Because learning C requires changing how you think about programs,
   not just memorizing rules, and that change takes time and effort.
   #+end_quote

2. What is the danger of focusing only on whether a C program “works”?
   #+begin_quote
   A program can produce output without the programmer understanding
   how or why it works, which prevents real learning and makes future
   problems harder.
   #+end_quote

3. Why is slowing down and thinking step by step important when
   writing or reading C code (unlike pseudocode)?
   #+begin_quote
   Because the computer executes instructions one at a time, and
   understanding C requires following that same step-by-step process.
   #+end_quote

4. What role do compiler messages and program output play in learning
   C?
   #+begin_quote
   They provide feedback about what the computer understood, helping
   programmers correct mistakes and adjust their thinking.
   #+end_quote

5. Why is learning C described as learning how the computer thinks
   rather than learning a set of commands?
   #+begin_quote
   Because C closely reflects how programs are executed, so
   understanding it requires reasoning about instructions, order, and
   effects rather than memorizing syntax.
   #+end_quote

** Hello World in OneCompiler.com

- Open onecompiler.com in your browser and follow the exercise.

- Alert me if anything doesn't work for you or if you fall behind.

- When we're done, upload the URL to Canvas: =In-class practice 2:
  Getting started with OneCompiler=.

* Week 3 ("Winter week"): Extra practice / Variables and Types
#+attr_html: :width 400px :float nil:
#+caption: Bryan Lake, January 28, 2026
[[../img/summary_practice_1_2.png]]

- [X] Winter week *highlights* from Google Chat.
- [ ] Snow practice *programs* "Story script" and "Invisible variables".
- [ ] Handcrafted *popquiz:* Think C! Chapter 2 - Variables and Types.
- [ ] *Quiz 2* is live: Use the editor when in doubt about code!
- [ ] *Lecture*: Introduction to C.
- [ ] *Practice*: Write and run "Hello World" in Google Cloud Shell.

** Snow week highlights (on Google chat)
#+attr_html: :width 400px :float nil:
#+caption: Bryan Lake, January 28, 2026
[[../img/lake.jpg]]

1. [[https://chat.google.com/room/AAAAIk8SqZE/nmTSwriVZis/nmTSwriVZis?cls=10][Follow the home programming assignments to the letter:]]
   #+begin_quote
   The Hello World program you were supposed to use was from the book.
   #+end_quote
2. [[https://chat.google.com/room/AAAAIk8SqZE/bEJo8mDRV00/bEJo8mDRV00?cls=10][The Dawn of Code by fireship - coding 101 (6 min):]]
   #+begin_quote
   Programming was never about writing code, but about thinking.
   #+end_quote
3. [[https://chat.google.com/room/AAAAIk8SqZE/7xSWib4K8kA/7xSWib4K8kA?cls=10][Learning strategies for programming beginners.]]
   #+begin_quote
   1) Short daily practice (15-30 min).
   2) Predict output -> run program -> explain output.
   3) Trace programs by hand.
   4) Rewrite programs from memory.
   5) Make only tiny changes to your programs at a time.
   #+end_quote
4. [[https://chat.google.com/room/AAAAIk8SqZE/I9rlsG07Kkw/I9rlsG07Kkw?cls=10][How long does it take to learn Python (or any language)?]]
   #+begin_quote
   200 hours for basics, 1200 hours for experts = 1-12 months.
   #+end_quote
5. [[https://chat.google.com/room/AAAAIk8SqZE/siQ3ist4toY/siQ3ist4toY?cls=10][If you have extra time, why not try to solve a bonus program?]]
   #+begin_quote
   Each successful bonus program will offset some bad quiz or a
   forgotten, or late submission. And they're often very simple!
   #+end_quote

** How to learn faster/better & example problem / Extra practice programs
#+attr_html: :width 400px :float nil:
[[../img/summary_1_overview.png]]

I posted this to the Google Chat, prompted by @JacieLynn, who asked
about winning strategies to learn how to program.

Here are a few study strategies that work well for beginners:
- *Short daily practice (15–30 minutes)* beats long sessions once a
  week. Aim for consistency.
- *Predict → run → explain:* Before you run a program, write down what
  you think it will print. Then run it. Then explain (in words) why it
  did that.
- *Trace by hand:* For small programs, go line-by-line and write the
  values of variables after each line. This is one of the fastest ways
  to “see” what the computer is doing.
- *Rewrite from memory:* After you understand an example, close it and
  retype it from scratch. Then compare. This builds independence
  quickly.
- *Make tiny changes (“one change only” rule)*: Change one thing (a
  number, a variable name, a print statement) and rerun. If you change
  multiple things at once, it’s hard to know what caused what.

At the moment, you can't do too much with the few tools you've got. If
you already read the second chapter, you know:

- ~main~ function
- ~printf~ function
- variables to store texts or numbers, e.g.: =age=
- basic types (~int~, ~double~, ~char~), e.g.: =int age=
- assignment, e.g. =int age = 22=

Here is a problem you can try:

*** Exercise: Getting Started with C Programs

These exercises assume you know:
- what a C program looks like (~main~, braces, semicolons),
- how to use ~printf~ *only with string literals*,
- that variables and basic types (~int~, ~double~, ~char~) exist,
but you do *not* yet know format specifiers or input.

Remember: A program can do meaningful work even if it does not display
that work yet.  Printing values is a separate skill that will come
later.

*** Problem 1: Program as a Script

Write a C program that prints a short “story” consisting of *at least
five lines*. Don't forget the program header.

Constraints:
- Each line must be printed using a separate ~printf~ statement.
- Each ~printf~ may only print a string literal.
- No variables are used in the output.

Example structure (content is up to you):
#+begin_example
- Program started.
- First step completed.
- Second step completed.
- Final step completed.
- Program finished.
#+end_example

Focus:
- Program structure
- Order of execution (top to bottom)
- Correct syntax (~printf~, quotes, semicolons, braces)

**** Sample solution

#+begin_src C :file story.c :main no :includes :results output :exports both
  // story.c: prints a short story of at least five lines.
  // input: none
  // output: Five strings each on one line that tell a story.
  // author: Marcus Birkenkrahe (pledged)
  // date: 1/24/2026

  #include <stdio.h>

  int main()
  {
    printf("Once upon a time there was a prince.\n");
    printf("He fell in love with a beautiful princess.\n");
    printf("But a dragon abducted the princess.\n");
    printf("The prince rescued the princess and brought her home.\n");
    printf("They married, had many children, and lived happily ever after.\n");
    return 0;
  }

#+end_src

#+RESULTS:
: Once upon a time there was a prince.
: He fell in love with a beautiful princess.
: But a dragon abducted the princess.
: The prince rescued the princess and brought her home.
: They married, had many children, and lived happily ever after.

*** Problem 2: Variables Exist Even If You Can’t See Them

Write a C program that:
- declares three variables:
  - one ~int~
  - one ~double~
  - one ~char~
- assigns a value to each variable
- prints messages that *describe* what the program is storing
- Don't forget the program header.

Constraints:
- Do NOT print the variable values.
- Each ~printf~ prints only a string literal.
- Variables must be declared and assigned even though their values are
  not shown.

Example output (exact wording not required):
#+begin_example
- An integer value has been stored.
- A floating-point value has been stored.
- A character value has been stored.
#+end_example

Focus:
- Declaring variables
- Assigning values
- Understanding that variables affect program state even without
  visible output

**** Sample solution

#+begin_src C :main no :includes :results output :export both :file invisible.c
  // invisible.c: describes the variable values that the program stored.
  // input: None.
  // output: Messages describing which values have been stored.
  // author: Marcus Birkenkrahe (pledged)
  // date: 1/24/2026

  #include <stdio.h>

  int main()
  {
    int one = 1; // store integer value
    printf("An integer value has been stored.\n");

    float one_half = 0.5f; // store floating-point value
    printf("A floating-point value has been stored.\n");

    char c = 'M'; // store character value
    printf("A character value has been stored.\n");

    return 0;
  }
#+end_src

#+RESULTS:
: An integer value has been stored.
: A floating-point value has been stored.
: A character value has been stored.

** Popquiz: Variables and Types (10 min)

10 minutes to answer the questions & then grade yourself and hand the
sheet in if you want your points to be counted as bonus (@ midterms).

1. Which placeholder is used in the ~printf~ function to print an
   integer value?
   #+begin_src C :main yes :includes <stdio.h> <stdlib.h> <string.h> :results output :exports both :noweb yes
     printf("This is an integer: %i.", 100); // or use %d
   #+end_src

   #+RESULTS:
   : This is an integer: 100.

2. What is a variable? How would you define it?
   #+begin_quote
   A *variable* is a *named* *location* (in computer memory) that stores a
   *value*.
   #+end_quote

3. What type of variable would be used to store the value ~'A'~?
   #+begin_src C :main yes :includes <stdio.h> <stdlib.h> <string.h> :results output :exports both :noweb yes
     char letter = 'A'; // `char` for character
     printf("You stored the character '%c'.", letter);
   #+end_src

   #+RESULTS:
   : You stored the character 'A'.

4. In the following *statement*, what's the *variable* name, what's the
   *value*, what's the variable *type*, and what's the *operator*?
   #+begin_example C++
   int age_of_bob = 22; // `;` is just the statement delimiter
   #+end_example

   #+begin_quote
   - ~int~ is the variable *type* (integer).
   - =age_of_bob= is the *variable* *name*.
   - ~=~ is the *operator* (assignment).
   - =22= is the variable *value* (integer).
   #+end_quote

5. True or false: "The computer performs addition and subtraction
   before it performs multiplication and division."
   #+begin_quote
   ~FALSE~. The rule of precedence from left to right is PEMDAS:
   Parentheses-Exponent-Multiplication-Division-Addition-Subtraction.
   #+end_quote

   #+begin_src C :main yes :includes <stdio.h> <stdlib.h> <string.h> :results output :exports both :noweb yes
     printf("%.0f", 5. + 3. * 4. / 2.); // 3 * 4 -> 12 | 12 / 2 = 6 | 5 + 6 = 11.
   #+end_src

   #+RESULTS:
   : 11

6. What will the code =printf("%c", 'a' + 1);= print as output?
   #+begin_src C :main yes :includes <stdio.h> <stdlib.h> <string.h> :results output :exports both :noweb yes
     printf("%c \n", 'a' + 1); // b
     printf("%i", 'a' + 1); // 98
   #+end_src

7. What's the difference in output between the two print statements?
   #+begin_src C :main yes :includes <stdio.h> :results output :exports both :noweb yes
     float speed = 100.;
     float time = 10;
     float distance = speed * time;

     printf("A car travelling %.0f mph travels %.0f miles in %.0f minutes.",
            speed, distance, time);
     printf("\nA car travelling %.0f mph travels %.0f miles in %.0f minutes.",
            100., speed * time, 10.);
   #+end_src

   #+RESULTS:
   : A car travelling 100 mph travels 1000 miles in 10 minutes.
   : A car travelling 100 mph travels 1000 miles in 10 minutes.

   #+begin_quote
   There is no difference in the output at all. The first ~printf~
   statement prints the =distance= computed before, the second computes
   the distance using a *composition* statement (uses simpler
   expressions).
   #+end_quote

8. For the last assignment, assign the values to the variables in the
   *state* diagram:
   #+begin_example
     speed      time     distance
   +-------+  +-------+  +-------+
   |       |  |       |  |       |
   |       |  |       |  |       |
   |       |  |       |  |       |
   +-------+  +-------+  +-------+
   #+end_example
   Solution:
   #+begin_example
     speed      time     distance
   +-------+  +-------+  +-------+
   |       |  |       |  |       |
   |  100  |  |  10   |  |  1000 |
   |       |  |       |  |       |
   +-------+  +-------+  +-------+
   #+end_example

9. What is ~\n~ and what does it do?
   #+begin_src C :main yes :includes <stdio.h> <stdlib.h> <string.h> :results output :exports both :noweb yes
     // printing a string with the escape character `\n'
     printf("Newline:\nAnd two more newlines\n\nOver.");
   #+end_src

   #+RESULTS:
   : Newline:
   : And two more newlines
   :
   : Over.

10. Write the source code of a minimal C program that will compile
    and, when run, print "Hello, world!" on the screen.

    #+begin_src C++ :results output :exports both
      #include <stdio.h>

      int main()
      {
        printf("Hello, world!"); // puts("Hello, world!");
        return 0;
      }
    #+end_src

    #+RESULTS:
    : Hello, world!

** Quiz - Instructions
#+attr_html: :width 400px :float nil:
[[../img/quiz.png]]
* Week 4: Compiling in Linux / Printing with ~printf~

- [X] *Review*: Popquiz
- [X] *Review:* "Hello world" commenting assignment.
- [X] *Practice*: editing, compiling, running C in Linux ([[https://youtu.be/H-UP_ogK95Y][video]])
- [X] *Lecture* (cont'd): Introduction to C/C++
- [X] *Lecture* + *Codealong* + *Practice*: printing values.

- [X] *Quiz 3*: Format and memory (deadline Feb 13).
- [X] *Practice 4*: upload your URL from the last class.
- [X] *Assignments*: Two programming assignments - print and convert.
- [X] *Review*: Last session & C++ printing preview

** DONE Review: Popquiz

This is the solution for the "hello world" program - most of you
forgot important details (syntax errors): the semi-colon at the end,
or to ~#include <stdio.h>~, or the curly brackets. *Details matter!*

The solution for the minimal C program that will print "Hello, World!"
on the screen:
#+begin_src C++ :results output
  #include <stdio.h>

  int main()
  {
    printf("Hello, world!");
  }
#+end_src

#+RESULTS:
: Hello, world!

** DONE Review: Hello world commenting assignment
#+attr_html: :width 400px :float nil:
#+caption: Screenshot of Submission Grader Helper GPT
[[../img/helloworld_grading.png]]

*Example comments:*

- *Program header name requirement:* The program header lists the name
  as =helloworld.c=, but the assignment requires the file/program name
  helloworld2.c.

- *Program header completeness:* The required characterization of input
  and output is present, but it does not match the actual program
  output (the output is no longer “Hello, World!”).

- *Function header requirement for ~main~:* The main function header does
  not follow the required format (separate lines for Function,
  Returns, and Parameters, with parameters explicitly listed as void).

- *Line-by-line commenting requirement:* Not every line is correctly
  explained; some explanations are inaccurate or misleading.

- *Incorrect comment (technical accuracy):* The comment claiming the
  message is “split by a ~\n~ function” is incorrect (~\n~ is an escape
  sequence, not a function).

- *Vague comments:* The comment for ~return(0);~ is vague and does not
  clearly explain what value is returned and what it signifies.

*You can resubmit this assignment with corrections within a week.*

** DONE Compilation practice

- We already covered OneCompiler.com
- Now we're moving to Google Cloud Shell
- The detailed notes are here: [[https://tinyurl.com/compilation-practice][tinyurl.com/compilation-practice]]
- There is a video where I show all the steps:
  [[https://tinyurl.com/compilation-practice-video][tinyurl.com/compilation-practice-video]]

** DONE Review - printing with =%s=
#+attr_html: :width 400px :float nil:
[[../img/printf.png]]

1. What is ="%s %s"= ?
   #+begin_quote
   Two format specifiers waiting for two string values, for example
   ="Hello"= and ="World"=.
   #+end_quote

2. What's the difference between =%s%s\n= and =%s %s \n= ?
   #+begin_quote
   =%s%s\n= prints two strings without a space followed by a new line.

   =%s %s \n= prints two strings with a space between them followed by a
   space followed by a new line.
   #+end_quote

   #+begin_src C :main yes :includes <stdio.h> <stdlib.h> <string.h> :results output :exports both :noweb yes
     printf("%s%s\n","hello","riders of Rohan");
     printf("%s %s \n hey","hello","riders of Rohan");
   #+end_src

   #+RESULTS:
   : helloriders of Rohan
   : hello riders of Rohan
   :  hey

3. How many ways are there to print two words on two lines?

   #+begin_quote
   Many. You saw at least three.
   #+end_quote

   #+begin_src C :results output :includes <stdio.h>
     // print with `puts` (newline included)
     puts("Hello,");
     puts("world!\n");  // this leads to two new lines
     // Print two strings, one call, newline in the format part
     printf("%s\n%s", "Hello,", "world!");
     // Print one string per call
     printf("\n\n%s", "Hello");
     printf("\n%s", "world!");
     // Print one string, newline in the value part
     printf("\n\n%s", "Hello\nworld!");
   #+end_src

   #+RESULTS:
   #+begin_example
   Hello,
   world!

   Hello,
   world!

   Hello
   world!

   Hello
   world!
   #+end_example

4. You want to print five numbers. How many format specifiers do you
   need?
   #+begin_quote
   Five - one for each number.
   #+end_quote
   #+begin_src C
     // Print five integer values
     printf("%i %i %i %i %i\n", 0, 1, 2, 3, 4);
   #+end_src

   #+RESULTS:
   : 0 1 2 3

5. How many arguments does =printf= accept?
   #+begin_quote
   As many as it takes & as much memory as there is available.
   The general form is:
   printf( [format string], [arguments] );
   #+end_quote

** DONE Lecture + Codealong + Practice: Printing values

Review of the last session:

1) Why does the following code not print =3.1459=?

   #+begin_src C :main yes :includes <stdio.h> <stdlib.h> <string.h> :results output :exports both :noweb yes
     printf(" %f ", 3.1459);
     printf(" %.4f ", 3.1459); // you need to set the correct precision
   #+end_src

   #+RESULTS:
   : 3.1459 

   How can you fix it?

2) What does the following code print?

   #+begin_src C :main yes :includes <stdio.h> <stdlib.h> <string.h> :results output :exports both :noweb yes
     // printf(" %i ", 'Birkenkrahe');
     printf(" %s ", "Birkenkrahe"); // need string format and argument
   #+end_src

   #+RESULTS:
   : Birkenkrahe 

   How can you fix it?

3) How can I get this code to print ="A"= instead of =A=?

   #+begin_src C :main yes :includes <stdio.h> <stdlib.h> <string.h> :results output :exports both :noweb yes
     printf(" \"%c\" ", 'A');
   #+end_src

   #+RESULTS:
   : "A" 

4) Preview: why we don't start with C++ (see =onecompiler.com/cpp=)

   #+begin_src C++ :main no :includes :results output :exports both :noweb yes
     #include <iostream> // instead of <stdio.h>

     int main()
     {
       printf("%f\n", 3.1459); // C-style printing
       
       std::cout << 3.1459 << std::endl;
         
       return 0;
     }
   #+end_src

   #+RESULTS:
   :  3.145900
   : 3.1459

* Week 5: Variable manipulation / Constants

- [X] *Lecture* + *Codealong* + *Practice*: Variables and types.
- [ ] *New bonus assignment:* Algebra with C 
- [ ] *Review*: Variables and types
- [ ] *Lecture + Codealong + Practice*: Constants
- [ ] *Review*: Constants

** DONE Review: Introduction to C/C++ and Shell Practice

1. What are the three components of a programming language?
   #+begin_quote
   Syntax (rules), semantics (meaning), and metadata (context).
   #+end_quote

2. What is the main difference between compiled and interpreted
   languages?
   #+begin_quote
   *Compiled* languages translate source code into machine code before
   execution, while *interpreted* languages execute source code
   directly.
   #+end_quote

3. What is the purpose of the ~#include~ directive?
   #+begin_quote
   It tells the compiler to include declarations from a *header file*,
   such as input/output functions (~<stdio.h>~) - preprocessing.
   #+end_quote

4. What role does the ~main~ function play in a C program?
   #+begin_quote
   It is the entry point where program execution begins.
   #+end_quote

5. What is the difference between a *code* and a *file* in OneCompiler?
   #+begin_quote
   A code is the online project container, while a file is the actual
   source file stored inside that project.
   #+end_quote

6. What file is produced by default when ~g++~ (compiler) successfully
   compiles a program?
   #+begin_quote
   An executable file named ~a.out~. This means it can be run by the PC.xs
   #+end_quote

7. Why does running ~a.out~ without ~./~ result in an error?
   #+begin_quote
   The current directory is not in the shell’s ~PATH~, so it must be
   specified explicitly (the shorthand is ~.~)
   #+end_quote

8. What type of error is a missing semicolon detected by the compiler?
   #+begin_quote
   A syntax error.
   #+end_quote

9. How does the computer represent a source code file internally?
   #+begin_quote
   As a sequence of bytes, each with a numeric value representing
   characters.
   #+end_quote

10. When you type =main= in the editor, what does the machine see?
    #+begin_example
    The machine sees a sequence of ASCII characters that correspond to
    a binary stream (bits):

    m: ASCII code = 109, binary = 01101101
    a: ASCII code = 97, binary = 01100001
    i: ASCII code = 105, binary = 01101001
    n: ASCII code = 110, binary = 01101110
    #+end_example

    #+begin_src C :results table
      printf("%c %c %c %c\n",'m','a','i','n');
      printf("%i %i %i %i\n",'m','a','i','n');
      printf("%b %b %b %b\n",'m','a','i','n');
    #+end_src

    #+RESULTS:
    |       m |       a |       i |       n |
    |     109 |      97 |     105 |     110 |
    | 1101101 | 1100001 | 1101001 | 1101110 |

11. What are the three main elements of computer architecture?
    #+attr_html: :width 500px:
    #+Caption: Central Processing Unit, Random Access Memory, Non Volatile Memory
    [[../img/computer_architecture.png]]

12. What's the main problem of computer architecture?
    #+Caption: The CPU is super-fast, the RAM is fast, and the NVM is very slow
    #+attr_html: :width 500px:
    [[../img/computer_architecture2.png]]

13. What does "tokenizing" mean?
    #+begin_quote
    "Tokenizing" means breaking down a sequence of text into smaller
    units called "tokens". In programming, these are the smallest
    elements of the source code that have meaning, such as keywords
    (e.g. =main=, =printf=), operators (=+=, ===), and punctuation =(;, {)=.
    #+end_quote

14. What's an IDE?
    #+begin_quote
    IDE = Integrated Development Environment - an application that
    integrates the editor, the compiler, and the shell so that you can
    write, compile and execute programs without leaving the app.

    Example: Emacs + Org-mode, or Onecompiler.com.
    #+end_quote

15. What is pseudocode?
    #+begin_quote
    Code in prose that encodes only the program logic and the program
    flow, but that does not comply with the syntax rules of any
    programming languages. A flowchart is pseudocode with graphics.
    #+end_quote

** DONE Lecture + Codealong + Practice: Variables and types

This material is still covered by the "Think C" chapter that you read
a couple of weeks ago. It might be worth reading it again now that you
understand every little bit of it.

** TODO Review of the last lecture - variables and types

1. What's wrong with this code?
   #+begin_src C
     height = 10;
     int height;
   #+end_src
   #+begin_quote
   The variable =height= is used before it is declared.
   #+end_quote

2. What's wrong with this code?
   #+begin_src C
     #include <stdio.h>
     int main(void) {
       int height;
       printf("The height is %d\n", height);
       return 0;
     }
   #+end_src

   #+RESULTS:
   : The height is 0

   #+begin_quote
   The variable =height= is uninitialized, so its value is undefined
   (even though computer says it's =0=, *this is not guaranteed*).
   #+end_quote

3. What's wrong with this code?
   #+begin_src C
     8 = height;
   #+end_src
   #+begin_quote
   *Literals* like =8= cannot be assigned values because they are
   literally constant, unchangeable, and must remain unchanged.
   #+end_quote

4. What's wrong with this multi-variable declaration/definition?
   #+begin_src C
     int height = 3, width = 2;
     int volume
   #+end_src
   #+begin_quote
   The statement is missing a semicolon (=;=).
   #+end_quote

5. What's wrong with this variable re-assignment?
   #+begin_src C
     int main(void) {
       int foo;
       foo = 8;
       int foo = 18;
       return 0;
     }
   #+end_src
   #+begin_quote
   A variable cannot be redeclared within the same *scope*. In this
   case, the scope is the area between ={= and =}=. Variables are said to
   be (or go) in or out of scope.
   #+end_quote

** NEXT Constants - three ways of defining and using them

Constants are hugely important: All scientific calculations depend on
them (see \pi), and all accounting and tax calculations (see tax rate).
